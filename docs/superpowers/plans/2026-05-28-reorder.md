# Réordonnancement ↑↓ Exercices / Blocs / Séries — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter des boutons ↑↓ permettant de réordonner exercices, blocs et séries dans l'écran de détail d'une séance, avec persistance immédiate en base.

**Architecture:** `swap(idA, idB)` atomique sur chaque repo → `reorderXxx(id, direction)` sur le service → 3 callbacks dans le hook → boutons ↑↓ dans `WorkoutExerciseCard` et `BlockCard`. Aucune lib supplémentaire.

**Tech Stack:** React Native / Expo SDK 54, expo-sqlite, TypeScript strict, Jest (jest-expo), Ionicons.

---

## File Map

| Fichier | Action |
|---|---|
| `app/repositories/IWorkoutExerciseRepository.ts` | Ajouter `swap` |
| `app/repositories/IBlockRepository.ts` | Ajouter `swap` |
| `app/repositories/ISetRepository.ts` | Ajouter `swap` |
| `app/repositories/InMemoryWorkoutExerciseRepository.ts` | Implémenter `swap` |
| `app/repositories/InMemoryBlockRepository.ts` | Implémenter `swap` |
| `app/repositories/InMemorySetRepository.ts` | Implémenter `swap` |
| `app/repositories/SQLiteWorkoutExerciseRepository.ts` | Implémenter `swap` |
| `app/repositories/SQLiteBlockRepository.ts` | Implémenter `swap` |
| `app/repositories/SQLiteSetRepository.ts` | Implémenter `swap` |
| `app/repositories/workoutExerciseRepository.contract.ts` | Ajouter `describe('swap')` |
| `app/repositories/blockRepository.contract.ts` | Ajouter `describe('swap')` |
| `app/repositories/setRepository.contract.ts` | Ajouter `describe('swap')` |
| `app/services/WorkoutExerciseService.ts` | Ajouter `reorderExercise`, `reorderBlock`, `reorderSet` |
| `app/services/WorkoutExerciseService.test.ts` | Ajouter `describe('reorderExercise/Block/Set')` |
| `app/hooks/useWorkoutExercises.ts` | Ajouter 3 callbacks + interface |
| `app/components/workout/WorkoutExerciseCard.tsx` | Nouvelles props + boutons ↑↓ exercices |
| `app/components/workout/BlockCard.tsx` | Nouvelles props + boutons ↑↓ blocs + refacto rows sets |
| `app/app/workout/[id].tsx` | Passer `isFirst`/`isLast` + callbacks reorder |

---

## Task 1 — Contract tests `swap` sur les 3 repos

**Files:**
- Modify: `app/repositories/workoutExerciseRepository.contract.ts`
- Modify: `app/repositories/blockRepository.contract.ts`
- Modify: `app/repositories/setRepository.contract.ts`

- [ ] **Step 1: Ajouter `describe('swap')` dans `workoutExerciseRepository.contract.ts`**

Ajouter à la fin de la fonction `runWorkoutExerciseRepositoryContractTests`, avant la fermeture `}` :

```typescript
describe('swap', () => {
  it('permute les order_index de deux workout_exercises', async () => {
    const a = await repo.save({ workout_id: 1, exercise_id: 1, order_index: 0 });
    const b = await repo.save({ workout_id: 1, exercise_id: 2, order_index: 1 });
    await repo.swap(a.id, b.id);
    expect((await repo.findById(a.id))!.order_index).toBe(1);
    expect((await repo.findById(b.id))!.order_index).toBe(0);
  });

  it('permute les order_index de deux items non-adjacents', async () => {
    const a = await repo.save({ workout_id: 1, exercise_id: 1, order_index: 0 });
    await repo.save({ workout_id: 1, exercise_id: 2, order_index: 1 });
    const c = await repo.save({ workout_id: 1, exercise_id: 3, order_index: 2 });
    await repo.swap(a.id, c.id);
    expect((await repo.findById(a.id))!.order_index).toBe(2);
    expect((await repo.findById(c.id))!.order_index).toBe(0);
  });

  it('throw si idB inconnu', async () => {
    const a = await repo.save({ workout_id: 1, exercise_id: 1, order_index: 0 });
    await expect(repo.swap(a.id, 999)).rejects.toThrow('999');
  });

  it('throw si idA inconnu', async () => {
    const a = await repo.save({ workout_id: 1, exercise_id: 1, order_index: 0 });
    await expect(repo.swap(999, a.id)).rejects.toThrow('999');
  });
});
```

- [ ] **Step 2: Ajouter `describe('swap')` dans `blockRepository.contract.ts`**

Dans `runBlockRepositoryContractTests`, ajouter à la fin (la fixture `bloc1` est déjà définie en haut du fichier : `{ workout_exercise_id: 1, name: 'Travail', order_index: 0, is_work_block: 1 }`) :

```typescript
describe('swap', () => {
  it('permute les order_index de deux blocs', async () => {
    const a = await repo.save({ ...bloc1, order_index: 0 });
    const b = await repo.save({ ...bloc1, name: 'Back-off', order_index: 1 });
    await repo.swap(a.id, b.id);
    expect((await repo.findById(a.id))!.order_index).toBe(1);
    expect((await repo.findById(b.id))!.order_index).toBe(0);
  });

  it('permute les order_index de deux blocs non-adjacents', async () => {
    const a = await repo.save({ ...bloc1, order_index: 0 });
    await repo.save({ ...bloc1, name: 'Milieu', order_index: 1 });
    const c = await repo.save({ ...bloc1, name: 'Back-off', order_index: 2 });
    await repo.swap(a.id, c.id);
    expect((await repo.findById(a.id))!.order_index).toBe(2);
    expect((await repo.findById(c.id))!.order_index).toBe(0);
  });

  it('throw si idB inconnu', async () => {
    const a = await repo.save({ ...bloc1, order_index: 0 });
    await expect(repo.swap(a.id, 999)).rejects.toThrow('999');
  });

  it('throw si idA inconnu', async () => {
    const a = await repo.save({ ...bloc1, order_index: 0 });
    await expect(repo.swap(999, a.id)).rejects.toThrow('999');
  });
});
```

- [ ] **Step 3: Ajouter `describe('swap')` dans `setRepository.contract.ts`**

Dans `runSetRepositoryContractTests`, ajouter à la fin (la fixture `serie1` est déjà définie : `{ block_id: 1, reps_min: 6, reps_max: 8, weight: null, weight_type: 'fixed', rest_duration: 120, order_index: 0 }`) :

```typescript
describe('swap', () => {
  it('permute les order_index de deux séries', async () => {
    const a = await repo.save({ ...serie1, order_index: 0 });
    const b = await repo.save({ ...serie1, order_index: 1 });
    await repo.swap(a.id, b.id);
    expect((await repo.findById(a.id))!.order_index).toBe(1);
    expect((await repo.findById(b.id))!.order_index).toBe(0);
  });

  it('permute les order_index de deux séries non-adjacentes', async () => {
    const a = await repo.save({ ...serie1, order_index: 0 });
    await repo.save({ ...serie1, order_index: 1 });
    const c = await repo.save({ ...serie1, order_index: 2 });
    await repo.swap(a.id, c.id);
    expect((await repo.findById(a.id))!.order_index).toBe(2);
    expect((await repo.findById(c.id))!.order_index).toBe(0);
  });

  it('throw si idB inconnu', async () => {
    const a = await repo.save({ ...serie1, order_index: 0 });
    await expect(repo.swap(a.id, 999)).rejects.toThrow('999');
  });

  it('throw si idA inconnu', async () => {
    const a = await repo.save({ ...serie1, order_index: 0 });
    await expect(repo.swap(999, a.id)).rejects.toThrow('999');
  });
});
```

- [ ] **Step 4: Vérifier que les tests échouent (méthode `swap` pas encore implémentée)**

```bash
cd app && npx jest --testPathPattern="InMemory(WorkoutExercise|Block|Set)Repository" --no-coverage 2>&1 | tail -20
```

Expected: `TypeError: repo.swap is not a function` ou `swap is not a function`.

---

## Task 2 — `swap` sur les 3 interfaces + InMemory

**Files:**
- Modify: `app/repositories/IWorkoutExerciseRepository.ts`
- Modify: `app/repositories/IBlockRepository.ts`
- Modify: `app/repositories/ISetRepository.ts`
- Modify: `app/repositories/InMemoryWorkoutExerciseRepository.ts`
- Modify: `app/repositories/InMemoryBlockRepository.ts`
- Modify: `app/repositories/InMemorySetRepository.ts`

- [ ] **Step 1: Ajouter `swap` dans les 3 interfaces**

`app/repositories/IWorkoutExerciseRepository.ts` — ajouter dans l'interface :
```typescript
swap(idA: number, idB: number): Promise<void>;
```

`app/repositories/IBlockRepository.ts` — ajouter dans l'interface :
```typescript
swap(idA: number, idB: number): Promise<void>;
```

`app/repositories/ISetRepository.ts` — ajouter dans l'interface :
```typescript
swap(idA: number, idB: number): Promise<void>;
```

- [ ] **Step 2: Implémenter `swap` dans `InMemoryWorkoutExerciseRepository`**

Ajouter après `delete` :

```typescript
async swap(idA: number, idB: number): Promise<void> {
  const a = this.items.find(i => i.id === idA);
  const b = this.items.find(i => i.id === idB);
  if (!a || !b) throw new Error(`swap: id inconnu (${idA}, ${idB})`);
  const tmp = a.order_index;
  a.order_index = b.order_index;
  b.order_index = tmp;
}
```

- [ ] **Step 3: Implémenter `swap` dans `InMemoryBlockRepository`**

Ajouter après `delete` :

```typescript
async swap(idA: number, idB: number): Promise<void> {
  const a = this.items.find(i => i.id === idA);
  const b = this.items.find(i => i.id === idB);
  if (!a || !b) throw new Error(`swap: id inconnu (${idA}, ${idB})`);
  const tmp = a.order_index;
  a.order_index = b.order_index;
  b.order_index = tmp;
}
```

- [ ] **Step 4: Implémenter `swap` dans `InMemorySetRepository`**

Ajouter après `delete` :

```typescript
async swap(idA: number, idB: number): Promise<void> {
  const a = this.items.find(i => i.id === idA);
  const b = this.items.find(i => i.id === idB);
  if (!a || !b) throw new Error(`swap: id inconnu (${idA}, ${idB})`);
  const tmp = a.order_index;
  a.order_index = b.order_index;
  b.order_index = tmp;
}
```

- [ ] **Step 5: Vérifier que les tests InMemory passent**

```bash
cd app && npx jest --testPathPattern="InMemory(WorkoutExercise|Block|Set)Repository" --no-coverage 2>&1 | tail -20
```

Expected: tous les tests passent (y compris les 4 nouveaux `swap` par repo = 12 nouveaux tests au total).

- [ ] **Step 6: Vérifier TypeScript**

```bash
cd app && npx tsc --noEmit 2>&1 | head -20
```

Expected: aucune erreur (les SQLite repos implémentant l'interface vont échouer ici — c'est ok pour l'instant, on les implémente en Task 3).

> Note : Si TypeScript se plaint des SQLite repos qui n'implémentent pas encore `swap`, c'est attendu. Continuer.

- [ ] **Step 7: Commit**

```bash
cd app && git add repositories/IWorkoutExerciseRepository.ts repositories/IBlockRepository.ts repositories/ISetRepository.ts repositories/InMemoryWorkoutExerciseRepository.ts repositories/InMemoryBlockRepository.ts repositories/InMemorySetRepository.ts repositories/workoutExerciseRepository.contract.ts repositories/blockRepository.contract.ts repositories/setRepository.contract.ts && git commit -m "feat(repo): swap atomique sur les 3 InMemory repos + contract tests"
```

---

## Task 3 — `swap` sur les 3 SQLite repos

**Files:**
- Modify: `app/repositories/SQLiteWorkoutExerciseRepository.ts`
- Modify: `app/repositories/SQLiteBlockRepository.ts`
- Modify: `app/repositories/SQLiteSetRepository.ts`

**Contexte :** Les SQLite repos utilisent `this.db` de type `SQLiteDatabase` (expo-sqlite). La méthode `withTransactionAsync(fn)` exécute `fn` dans une transaction atomique. Chaque repo connaît sa propre table.

- [ ] **Step 1: Implémenter `swap` dans `SQLiteWorkoutExerciseRepository`**

Ajouter après `delete` :

```typescript
async swap(idA: number, idB: number): Promise<void> {
  const a = await this.findById(idA);
  const b = await this.findById(idB);
  if (!a || !b) throw new Error(`swap: id inconnu (${idA}, ${idB})`);
  await this.db.withTransactionAsync(async () => {
    await this.db.runAsync(
      'UPDATE workout_exercises SET order_index=? WHERE id=?',
      [b.order_index, idA]
    );
    await this.db.runAsync(
      'UPDATE workout_exercises SET order_index=? WHERE id=?',
      [a.order_index, idB]
    );
  });
}
```

- [ ] **Step 2: Implémenter `swap` dans `SQLiteBlockRepository`**

Ajouter après `delete` :

```typescript
async swap(idA: number, idB: number): Promise<void> {
  const a = await this.findById(idA);
  const b = await this.findById(idB);
  if (!a || !b) throw new Error(`swap: id inconnu (${idA}, ${idB})`);
  await this.db.withTransactionAsync(async () => {
    await this.db.runAsync(
      'UPDATE blocks SET order_index=? WHERE id=?',
      [b.order_index, idA]
    );
    await this.db.runAsync(
      'UPDATE blocks SET order_index=? WHERE id=?',
      [a.order_index, idB]
    );
  });
}
```

- [ ] **Step 3: Implémenter `swap` dans `SQLiteSetRepository`**

Ajouter après `delete` :

```typescript
async swap(idA: number, idB: number): Promise<void> {
  const a = await this.findById(idA);
  const b = await this.findById(idB);
  if (!a || !b) throw new Error(`swap: id inconnu (${idA}, ${idB})`);
  await this.db.withTransactionAsync(async () => {
    await this.db.runAsync(
      'UPDATE sets SET order_index=? WHERE id=?',
      [b.order_index, idA]
    );
    await this.db.runAsync(
      'UPDATE sets SET order_index=? WHERE id=?',
      [a.order_index, idB]
    );
  });
}
```

- [ ] **Step 4: Vérifier TypeScript — zéro erreur attendue**

```bash
cd app && npx tsc --noEmit 2>&1 | head -20
```

Expected: aucune erreur.

- [ ] **Step 5: Lancer tous les tests**

```bash
cd app && npx jest --no-coverage 2>&1 | tail -20
```

Expected: tous les tests passent.

- [ ] **Step 6: Commit**

```bash
cd app && git add repositories/SQLiteWorkoutExerciseRepository.ts repositories/SQLiteBlockRepository.ts repositories/SQLiteSetRepository.ts && git commit -m "feat(repo): swap atomique sur les 3 SQLite repos"
```

---

## Task 4 — Service : 3 méthodes `reorderXxx`

**Files:**
- Modify: `app/services/WorkoutExerciseService.ts`
- Modify: `app/services/WorkoutExerciseService.test.ts`

**Contexte :** `WorkoutExerciseService` a déjà 9 méthodes publiques (addToWorkout, getWithDetails, remove, updateSet, addSet, removeSet, addBlock, updateBlock, removeBlock). On ajoute 3 méthodes reorder. Les repos InMemory sont utilisés dans les tests (pas de SQLite).

- [ ] **Step 1: Écrire les tests de service (Red)**

Ajouter à la fin de `WorkoutExerciseService.test.ts`, avant la fermeture du `describe('WorkoutExerciseService')` :

```typescript
describe('reorderExercise', () => {
  it('monte un exercice : permute son order_index avec celui du précédent', async () => {
    const { service, exerciseRepo } = makeService();
    const ex1 = await seedExercise(exerciseRepo);
    const ex2 = await exerciseRepo.save({ ...ex1, name: 'Squat' });
    const d1 = await service.addToWorkout(1, ex1.id);
    const d2 = await service.addToWorkout(1, ex2.id);
    // d1.order_index = 0, d2.order_index = 1
    await service.reorderExercise(1, d2.id, 'up');
    const details = await service.getWithDetails(1);
    const sorted = details.sort((a, b) => a.order_index - b.order_index);
    expect(sorted[0].id).toBe(d2.id);
    expect(sorted[1].id).toBe(d1.id);
  });

  it('no-op si exercice déjà en première position et direction "up"', async () => {
    const { service, exerciseRepo } = makeService();
    const ex1 = await seedExercise(exerciseRepo);
    const d1 = await service.addToWorkout(1, ex1.id);
    await service.reorderExercise(1, d1.id, 'up');
    const details = await service.getWithDetails(1);
    expect(details[0].order_index).toBe(0);
  });

  it('no-op si exercice déjà en dernière position et direction "down"', async () => {
    const { service, exerciseRepo } = makeService();
    const ex1 = await seedExercise(exerciseRepo);
    const ex2 = await exerciseRepo.save({ ...ex1, name: 'Squat' });
    await service.addToWorkout(1, ex1.id);
    const d2 = await service.addToWorkout(1, ex2.id);
    await service.reorderExercise(1, d2.id, 'down');
    const details = await service.getWithDetails(1);
    expect(details.find(d => d.id === d2.id)!.order_index).toBe(1);
  });

  it('no-op si id inconnu dans la séance (pas de throw)', async () => {
    const { service, exerciseRepo } = makeService();
    const ex1 = await seedExercise(exerciseRepo);
    await service.addToWorkout(1, ex1.id);
    await expect(service.reorderExercise(1, 999, 'up')).resolves.toBeUndefined();
  });
});

describe('reorderBlock', () => {
  it('monte un bloc : permute son order_index avec celui du précédent', async () => {
    const { service, exerciseRepo } = makeService();
    const exercise = await seedExercise(exerciseRepo);
    const detail = await service.addToWorkout(1, exercise.id);
    await service.addBlock(detail.id, 'Back-off', 0);
    const before = await service.getWithDetails(1);
    const b0 = before[0].blocks[0]; // order_index 0
    const b1 = before[0].blocks[1]; // order_index 1
    await service.reorderBlock(detail.id, b1.id, 'up');
    const after = await service.getWithDetails(1);
    const sorted = after[0].blocks.sort((a, b) => a.order_index - b.order_index);
    expect(sorted[0].id).toBe(b1.id);
    expect(sorted[1].id).toBe(b0.id);
  });

  it('no-op si bloc en première position et direction "up"', async () => {
    const { service, exerciseRepo } = makeService();
    const exercise = await seedExercise(exerciseRepo);
    const detail = await service.addToWorkout(1, exercise.id);
    const blockId = detail.blocks[0].id;
    await service.reorderBlock(detail.id, blockId, 'up');
    const after = await service.getWithDetails(1);
    expect(after[0].blocks[0].order_index).toBe(0);
  });

  it('no-op si id inconnu (pas de throw)', async () => {
    const { service, exerciseRepo } = makeService();
    const exercise = await seedExercise(exerciseRepo);
    const detail = await service.addToWorkout(1, exercise.id);
    await expect(service.reorderBlock(detail.id, 999, 'up')).resolves.toBeUndefined();
  });
});

describe('reorderSet', () => {
  it('monte une série : permute son order_index avec celui de la précédente', async () => {
    const { service, exerciseRepo } = makeService();
    const exercise = await seedExercise(exerciseRepo);
    const detail = await service.addToWorkout(1, exercise.id);
    const blockId = detail.blocks[0].id;
    await service.addSet(blockId);
    const before = await service.getWithDetails(1);
    const s0 = before[0].blocks[0].sets[0]; // order_index 0
    const s1 = before[0].blocks[0].sets[1]; // order_index 1
    await service.reorderSet(blockId, s1.id, 'up');
    const after = await service.getWithDetails(1);
    const sorted = after[0].blocks[0].sets.sort((a, b) => a.order_index - b.order_index);
    expect(sorted[0].id).toBe(s1.id);
    expect(sorted[1].id).toBe(s0.id);
  });

  it('no-op si série en première position et direction "up"', async () => {
    const { service, exerciseRepo } = makeService();
    const exercise = await seedExercise(exerciseRepo);
    const detail = await service.addToWorkout(1, exercise.id);
    const blockId = detail.blocks[0].id;
    const setId = detail.blocks[0].sets[0].id;
    await service.reorderSet(blockId, setId, 'up');
    const after = await service.getWithDetails(1);
    expect(after[0].blocks[0].sets[0].order_index).toBe(0);
  });

  it('no-op si id inconnu (pas de throw)', async () => {
    const { service, exerciseRepo } = makeService();
    const exercise = await seedExercise(exerciseRepo);
    const detail = await service.addToWorkout(1, exercise.id);
    const blockId = detail.blocks[0].id;
    await expect(service.reorderSet(blockId, 999, 'up')).resolves.toBeUndefined();
  });
});
```

- [ ] **Step 2: Vérifier que les tests échouent (Red)**

```bash
cd app && npx jest WorkoutExerciseService --no-coverage 2>&1 | tail -15
```

Expected: `reorderExercise is not a function` (ou similar).

- [ ] **Step 3: Implémenter les 3 méthodes dans `WorkoutExerciseService.ts`**

Ajouter après `removeBlock` (ligne 127), avant la méthode privée `loadDetail` :

```typescript
async reorderExercise(workoutId: number, exerciseId: number, direction: 'up' | 'down'): Promise<void> {
  const siblings = (await this.weRepo.findByWorkoutId(workoutId))
    .sort((a, b) => a.order_index - b.order_index);
  const idx = siblings.findIndex(e => e.id === exerciseId);
  if (idx === -1) return;
  const neighborIdx = direction === 'up' ? idx - 1 : idx + 1;
  if (neighborIdx < 0 || neighborIdx >= siblings.length) return;
  await this.weRepo.swap(siblings[idx].id, siblings[neighborIdx].id);
}

async reorderBlock(workoutExerciseId: number, blockId: number, direction: 'up' | 'down'): Promise<void> {
  const siblings = (await this.blockRepo.findByWorkoutExerciseId(workoutExerciseId))
    .sort((a, b) => a.order_index - b.order_index);
  const idx = siblings.findIndex(b => b.id === blockId);
  if (idx === -1) return;
  const neighborIdx = direction === 'up' ? idx - 1 : idx + 1;
  if (neighborIdx < 0 || neighborIdx >= siblings.length) return;
  await this.blockRepo.swap(siblings[idx].id, siblings[neighborIdx].id);
}

async reorderSet(blockId: number, setId: number, direction: 'up' | 'down'): Promise<void> {
  const siblings = (await this.setRepo.findByBlockId(blockId))
    .sort((a, b) => a.order_index - b.order_index);
  const idx = siblings.findIndex(s => s.id === setId);
  if (idx === -1) return;
  const neighborIdx = direction === 'up' ? idx - 1 : idx + 1;
  if (neighborIdx < 0 || neighborIdx >= siblings.length) return;
  await this.setRepo.swap(siblings[idx].id, siblings[neighborIdx].id);
}
```

- [ ] **Step 4: Vérifier que tous les tests passent (Green)**

```bash
cd app && npx jest --no-coverage 2>&1 | tail -10
```

Expected: tous les tests passent.

- [ ] **Step 5: Vérifier TypeScript**

```bash
cd app && npx tsc --noEmit 2>&1 | head -20
```

Expected: aucune erreur.

- [ ] **Step 6: Commit**

```bash
cd app && git add services/WorkoutExerciseService.ts services/WorkoutExerciseService.test.ts && git commit -m "feat(service): reorderExercise, reorderBlock, reorderSet"
```

---

## Task 5 — Hook : 3 callbacks reorder

**Files:**
- Modify: `app/hooks/useWorkoutExercises.ts`

**Contexte :** Le hook expose déjà `add`, `remove`, `refresh`, `updateSet`, `addSet`, `removeSet`, `addBlock`, `updateBlock`, `removeBlock`. Chaque callback suit le même pattern :
```typescript
const xxx = useCallback(async (...args) => {
  try {
    await service.xxx(...args);
    await refresh();
  } catch (e) {
    const msg = e instanceof Error ? e.message : 'Erreur inconnue';
    if (mountedRef.current) setError(msg);
    throw e;
  }
}, [service, refresh]);
```

- [ ] **Step 1: Ajouter les 3 types dans `UseWorkoutExercisesResult`**

Dans l'interface `UseWorkoutExercisesResult` (lignes 11-24), ajouter après `removeBlock` :

```typescript
reorderExercise: (exerciseId: number, direction: 'up' | 'down') => Promise<void>;
reorderBlock: (workoutExerciseId: number, blockId: number, direction: 'up' | 'down') => Promise<void>;
reorderSet: (blockId: number, setId: number, direction: 'up' | 'down') => Promise<void>;
```

- [ ] **Step 2: Ajouter les 3 callbacks dans le corps du hook**

Ajouter après `removeBlock` (ligne 150), avant le `return` :

```typescript
const reorderExercise = useCallback(async (exerciseId: number, direction: 'up' | 'down') => {
  try {
    await service.reorderExercise(workoutId, exerciseId, direction);
    await refresh();
  } catch (e) {
    const msg = e instanceof Error ? e.message : 'Erreur inconnue';
    if (mountedRef.current) setError(msg);
    throw e;
  }
}, [service, workoutId, refresh]);

const reorderBlock = useCallback(async (workoutExerciseId: number, blockId: number, direction: 'up' | 'down') => {
  try {
    await service.reorderBlock(workoutExerciseId, blockId, direction);
    await refresh();
  } catch (e) {
    const msg = e instanceof Error ? e.message : 'Erreur inconnue';
    if (mountedRef.current) setError(msg);
    throw e;
  }
}, [service, refresh]);

const reorderSet = useCallback(async (blockId: number, setId: number, direction: 'up' | 'down') => {
  try {
    await service.reorderSet(blockId, setId, direction);
    await refresh();
  } catch (e) {
    const msg = e instanceof Error ? e.message : 'Erreur inconnue';
    if (mountedRef.current) setError(msg);
    throw e;
  }
}, [service, refresh]);
```

- [ ] **Step 3: Ajouter les 3 callbacks dans le `return`**

Modifier la ligne `return { exercises, loading, error, add, remove, refresh, ... }` pour inclure les 3 nouveaux callbacks :

```typescript
return {
  exercises, loading, error,
  add, remove, refresh,
  updateSet, addSet, removeSet,
  addBlock, updateBlock, removeBlock,
  reorderExercise, reorderBlock, reorderSet,
};
```

- [ ] **Step 4: Vérifier TypeScript**

```bash
cd app && npx tsc --noEmit 2>&1 | head -20
```

Expected: aucune erreur.

- [ ] **Step 5: Lancer tous les tests**

```bash
cd app && npx jest --no-coverage 2>&1 | tail -10
```

Expected: tous les tests passent.

- [ ] **Step 6: Commit**

```bash
cd app && git add hooks/useWorkoutExercises.ts && git commit -m "feat(hook): reorderExercise, reorderBlock, reorderSet callbacks"
```

---

## Task 6 — UI : boutons ↑↓ exercices dans `WorkoutExerciseCard` + `workout/[id].tsx`

**Files:**
- Modify: `app/components/workout/WorkoutExerciseCard.tsx`
- Modify: `app/app/workout/[id].tsx`

**Contexte :** Le header de `WorkoutExerciseCard` est un `PressableA11y` avec `flexDirection: 'row'`. Il contient `[headerContent (flex:1)] [chevron expand/collapse]`. On ajoute `[↑] [↓]` entre `headerContent` et le chevron expand. Les boutons sont conditionnels (`!isFirst` / `!isLast`). `PressableA11y` enforce `accessibilityLabel` au niveau TypeScript et garantit `minHeight: 44` / `minWidth: 44` — pas besoin de style supplémentaire pour ça, mais on ajoute quand même un style `reorderBtn` pour le centrage.

- [ ] **Step 1: Ajouter les 4 nouvelles props dans `WorkoutExerciseCardProps`**

Dans `app/components/workout/WorkoutExerciseCard.tsx`, modifier l'interface :

```typescript
interface WorkoutExerciseCardProps {
  detail: WorkoutExerciseDetail;
  onRemove: () => void;
  onUpdateSet: (setId: number, dto: UpdateSetDto) => Promise<void>;
  onAddSet: (blockId: number) => Promise<void>;
  onRemoveSet: (setId: number) => Promise<void>;
  onAddBlock: (workoutExerciseId: number, name: string, isWorkBlock: 0 | 1) => Promise<void>;
  onUpdateBlock: (blockId: number, dto: UpdateBlockDto) => Promise<void>;
  onRemoveBlock: (blockId: number) => Promise<void>;
  isFirst: boolean;
  isLast: boolean;
  onMoveUp: () => Promise<void>;
  onMoveDown: () => Promise<void>;
}
```

- [ ] **Step 2: Déstructurer les nouvelles props dans le composant**

Modifier la ligne de déstructuration dans `WorkoutExerciseCard` :

```typescript
export function WorkoutExerciseCard({
  detail,
  onRemove,
  onUpdateSet,
  onAddSet,
  onRemoveSet,
  onAddBlock,
  onUpdateBlock,
  onRemoveBlock,
  isFirst,
  isLast,
  onMoveUp,
  onMoveDown,
}: WorkoutExerciseCardProps) {
```

- [ ] **Step 3: Restructurer le header — remplacer le `PressableA11y` wrapper par un `View` row**

Actuellement, tout le header est un seul `PressableA11y`. Pour éviter les conflits touch avec les boutons ↑↓ imbriqués, on divise le header en un `View` row contenant :
- un `PressableA11y` pour la zone expand/collapse (nom + chevron)
- les boutons ↑↓ comme frères (pas enfants) de ce Pressable

Remplacer le bloc `<PressableA11y ... style={styles.header}>...</PressableA11y>` par :

```tsx
<View style={styles.header}>
  <PressableA11y
    accessibilityLabel={`${detail.exercise.name}, ${expanded ? 'réduire' : 'développer'}`}
    accessibilityHint="Appuyer longuement pour supprimer"
    accessibilityState={{ expanded }}
    onPress={() => setExpanded(e => !e)}
    onLongPress={handleLongPress}
    style={styles.headerMain}
  >
    <View style={styles.headerContent}>
      <Text style={[styles.name, { color: colors.text }]} numberOfLines={1}>
        {detail.exercise.name}
      </Text>
      {muscleGroups ? (
        <Text style={[styles.muscles, { color: colors.textSecondary }]} numberOfLines={1}>
          {muscleGroups}
        </Text>
      ) : null}
    </View>
    <Ionicons
      name={expanded ? 'chevron-up' : 'chevron-down'}
      size={18}
      color={colors.textSecondary}
      importantForAccessibility="no"
      accessibilityElementsHidden={true}
    />
  </PressableA11y>
  {!isFirst && (
    <PressableA11y
      accessibilityLabel={`Monter ${detail.exercise.name}`}
      onPress={onMoveUp}
      style={styles.reorderBtn}
    >
      <Ionicons name="chevron-up-outline" size={16} color={colors.textSecondary} />
    </PressableA11y>
  )}
  {!isLast && (
    <PressableA11y
      accessibilityLabel={`Descendre ${detail.exercise.name}`}
      onPress={onMoveDown}
      style={styles.reorderBtn}
    >
      <Ionicons name="chevron-down-outline" size={16} color={colors.textSecondary} />
    </PressableA11y>
  )}
</View>
```

- [ ] **Step 4: Mettre à jour les styles du header**

Remplacer le style `header` existant et ajouter `headerMain` + `reorderBtn` :

```typescript
// Avant :
header: { flexDirection: 'row', alignItems: 'center', padding: 16, gap: 8, minHeight: 56 },

// Après :
header: { flexDirection: 'row', alignItems: 'center', paddingRight: 4, minHeight: 56 },
headerMain: { flex: 1, flexDirection: 'row', alignItems: 'center', paddingLeft: 16, paddingVertical: 16, paddingRight: 8, gap: 8 },
reorderBtn: { alignItems: 'center', justifyContent: 'center' },
```

Note : `PressableA11y` impose déjà `minHeight: 44` et `minWidth: 44`.

- [ ] **Step 5: Brancher dans `workout/[id].tsx`**

Le hook expose maintenant `reorderExercise`. Dans le destructuring :

```typescript
const {
  exercises,
  loading,
  error,
  remove,
  refresh,
  updateSet,
  addSet,
  removeSet,
  addBlock,
  updateBlock,
  removeBlock,
  reorderExercise,
} = useWorkoutExercises(workoutId);
```

Dans le `renderItem` de la `FlatList` :

```tsx
renderItem={({ item, index }) => (
  <WorkoutExerciseCard
    detail={item}
    isFirst={index === 0}
    isLast={index === exercises.length - 1}
    onMoveUp={() => reorderExercise(item.id, 'up')}
    onMoveDown={() => reorderExercise(item.id, 'down')}
    onRemove={() => confirmRemove(item)}
    onUpdateSet={updateSet}
    onAddSet={addSet}
    onRemoveSet={removeSet}
    onAddBlock={addBlock}
    onUpdateBlock={updateBlock}
    onRemoveBlock={removeBlock}
  />
)}
```

- [ ] **Step 6: Vérifier TypeScript**

```bash
cd app && npx tsc --noEmit 2>&1 | head -20
```

Expected: aucune erreur.

- [ ] **Step 7: Lancer tous les tests**

```bash
cd app && npx jest --no-coverage 2>&1 | tail -10
```

Expected: tous les tests passent.

- [ ] **Step 8: Commit**

```bash
cd app && git add components/workout/WorkoutExerciseCard.tsx app/workout/[id].tsx && git commit -m "feat(ui): boutons réordonnancement ↑↓ exercices"
```

---

## Task 7 — UI : boutons ↑↓ blocs + refacto rows sets dans `BlockCard` + liaison `WorkoutExerciseCard`

**Files:**
- Modify: `app/components/workout/BlockCard.tsx`
- Modify: `app/components/workout/WorkoutExerciseCard.tsx`

**Contexte :** `BlockCard` doit recevoir des props pour le reorder des blocs ET exposer les ↑↓ pour les séries. Il reçoit aussi `onReorderSet` depuis `WorkoutExerciseCard`. Les set rows actuels sont des `PressableA11y` standalone — ils deviennent des `View` row contenant le pressable + les boutons ↑↓.

Le composant `BlockCard` reçoit ces nouvelles props :
- `isFirst: boolean` / `isLast: boolean` — pour les boutons ↑↓ du bloc lui-même
- `onMoveUp: () => Promise<void>` / `onMoveDown: () => Promise<void>` — reorder du bloc
- `onReorderSet: (setId: number, direction: 'up' | 'down') => Promise<void>` — reorder d'une série

- [ ] **Step 1: Étendre `BlockCardProps`**

Remplacer l'interface complète :

```typescript
interface BlockCardProps {
  block: BlockWithSets;
  onUpdateSet: (setId: number, dto: UpdateSetDto) => Promise<void>;
  onAddSet: (blockId: number) => Promise<void>;
  onRemoveSet: (setId: number) => Promise<void>;
  onRenameBlock: (block: BlockWithSets) => void;
  onRemoveBlock: (blockId: number) => Promise<void>;
  isFirst: boolean;
  isLast: boolean;
  onMoveUp: () => Promise<void>;
  onMoveDown: () => Promise<void>;
  onReorderSet: (setId: number, direction: 'up' | 'down') => Promise<void>;
}
```

- [ ] **Step 2: Déstructurer les nouvelles props**

```typescript
export function BlockCard({
  block,
  onUpdateSet,
  onAddSet,
  onRemoveSet,
  onRenameBlock,
  onRemoveBlock,
  isFirst,
  isLast,
  onMoveUp,
  onMoveDown,
  onReorderSet,
}: BlockCardProps) {
```

- [ ] **Step 3: Ajouter les boutons ↑↓ sur la ligne nom de bloc**

Actuellement le nom de bloc est un `PressableA11y` standalone. Il faut envelopper dans une `View` row pour ajouter les boutons à droite :

```tsx
<View style={styles.blockHeader}>
  <PressableA11y
    accessibilityLabel={`${block.name}, appuyer longuement pour modifier`}
    accessibilityHint="Appuyer longuement pour renommer ou supprimer"
    onPress={() => {}}
    onLongPress={handleBlockLongPress}
    style={styles.blockNamePressable}
  >
    <Text style={[styles.blockName, { color: colors.textSecondary }]}>{block.name}</Text>
  </PressableA11y>
  {!isFirst && (
    <PressableA11y
      accessibilityLabel={`Monter le bloc ${block.name}`}
      onPress={onMoveUp}
      style={styles.reorderBtn}
    >
      <Ionicons name="chevron-up-outline" size={14} color={colors.textSecondary} />
    </PressableA11y>
  )}
  {!isLast && (
    <PressableA11y
      accessibilityLabel={`Descendre le bloc ${block.name}`}
      onPress={onMoveDown}
      style={styles.reorderBtn}
    >
      <Ionicons name="chevron-down-outline" size={14} color={colors.textSecondary} />
    </PressableA11y>
  )}
</View>
```

- [ ] **Step 4: Refactorer les set rows avec boutons ↑↓**

Les sets actuels :
```tsx
block.sets.map((set) => (
  <PressableA11y key={set.id} ... >
    <Text ...>{formatSet(set)}</Text>
  </PressableA11y>
))
```

Deviennent :
```tsx
block.sets.map((set, index) => (
  <View key={set.id} style={styles.setRow}>
    <PressableA11y
      accessibilityLabel={`${formatSet(set)}, appuyer pour modifier`}
      accessibilityHint="Appuyer longuement pour supprimer"
      onPress={() => setEditingSet(set)}
      onLongPress={() => handleSetLongPress(set)}
      style={styles.setMain}
    >
      <Text style={[styles.set, { color: colors.text }]}>
        {formatSet(set)}
      </Text>
    </PressableA11y>
    {index > 0 && (
      <PressableA11y
        accessibilityLabel={`Monter série ${index + 1}`}
        onPress={() => onReorderSet(set.id, 'up')}
        style={styles.reorderBtn}
      >
        <Ionicons name="chevron-up-outline" size={14} color={colors.textSecondary} />
      </PressableA11y>
    )}
    {index < block.sets.length - 1 && (
      <PressableA11y
        accessibilityLabel={`Descendre série ${index + 1}`}
        onPress={() => onReorderSet(set.id, 'down')}
        style={styles.reorderBtn}
      >
        <Ionicons name="chevron-down-outline" size={14} color={colors.textSecondary} />
      </PressableA11y>
    )}
  </View>
))
```

- [ ] **Step 5: Ajouter les nouveaux styles dans `BlockCard`**

Ajouter dans `StyleSheet.create` :

```typescript
blockHeader: { flexDirection: 'row', alignItems: 'center' },
blockNamePressable: { flex: 1 },
setRow: { flexDirection: 'row', alignItems: 'center' },
setMain: { flex: 1 },
reorderBtn: { alignItems: 'center', justifyContent: 'center' },
```

- [ ] **Step 6: Brancher les nouvelles props depuis `WorkoutExerciseCard`**

Dans `WorkoutExerciseCard`, ajouter les destructuring des callbacks hook et modifier le `.map` des blocs.

Ajouter `onReorderSet` et `onReorderBlock` aux props de `WorkoutExerciseCard` :

```typescript
interface WorkoutExerciseCardProps {
  // ... props existantes ...
  onReorderBlock: (workoutExerciseId: number, blockId: number, direction: 'up' | 'down') => Promise<void>;
  onReorderSet: (blockId: number, setId: number, direction: 'up' | 'down') => Promise<void>;
}
```

Déstructurer :
```typescript
export function WorkoutExerciseCard({
  // ... existants ...
  onReorderBlock,
  onReorderSet,
}: WorkoutExerciseCardProps) {
```

Dans le `.map` des blocs (actuellement `detail.blocks.map(block => (...))`) :

```tsx
detail.blocks.map((block, index) => (
  <BlockCard
    key={block.id}
    block={block}
    isFirst={index === 0}
    isLast={index === detail.blocks.length - 1}
    onMoveUp={() => onReorderBlock(detail.id, block.id, 'up')}
    onMoveDown={() => onReorderBlock(detail.id, block.id, 'down')}
    onReorderSet={(setId, dir) => onReorderSet(block.id, setId, dir)}
    onUpdateSet={onUpdateSet}
    onAddSet={onAddSet}
    onRemoveSet={onRemoveSet}
    onRenameBlock={handleRenameBlock}
    onRemoveBlock={onRemoveBlock}
  />
))
```

- [ ] **Step 7: Mettre à jour `workout/[id].tsx`** pour passer les 2 nouvelles props à `WorkoutExerciseCard`

Dans le destructuring du hook :
```typescript
const {
  // ... existants ...
  reorderExercise,
  reorderBlock,
  reorderSet,
} = useWorkoutExercises(workoutId);
```

Dans `renderItem` :
```tsx
<WorkoutExerciseCard
  detail={item}
  isFirst={index === 0}
  isLast={index === exercises.length - 1}
  onMoveUp={() => reorderExercise(item.id, 'up')}
  onMoveDown={() => reorderExercise(item.id, 'down')}
  onReorderBlock={reorderBlock}
  onReorderSet={reorderSet}
  onRemove={() => confirmRemove(item)}
  onUpdateSet={updateSet}
  onAddSet={addSet}
  onRemoveSet={removeSet}
  onAddBlock={addBlock}
  onUpdateBlock={updateBlock}
  onRemoveBlock={removeBlock}
/>
```

- [ ] **Step 8: Vérifier TypeScript**

```bash
cd app && npx tsc --noEmit 2>&1 | head -20
```

Expected: aucune erreur.

- [ ] **Step 9: Lancer tous les tests**

```bash
cd app && npx jest --no-coverage 2>&1 | tail -10
```

Expected: tous les tests passent.

- [ ] **Step 10: Commit**

```bash
cd app && git add components/workout/WorkoutExerciseCard.tsx components/workout/BlockCard.tsx app/workout/[id].tsx && git commit -m "feat(ui): boutons réordonnancement ↑↓ blocs et séries"
```

---

## Task 8 — Vérification finale

- [ ] **Step 1: Lancer la suite de tests complète**

```bash
cd app && npx jest --no-coverage 2>&1 | tail -15
```

Expected: tous les tests passent. Vérifier le total — avant cette session il y avait 120 tests, on devrait en avoir ~142 (120 + 12 contrats swap + 10 tests service).

- [ ] **Step 2: Vérifier TypeScript**

```bash
cd app && npx tsc --noEmit 2>&1
```

Expected: aucune sortie (0 erreurs).

- [ ] **Step 3: Commit final si nécessaire**

Si le step précédent détecte des fichiers non commités :
```bash
cd app && git status
```

Commiter tout ce qui reste.
